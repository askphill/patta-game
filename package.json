{
  "name": "patta-game-concept",
  "private": true,
  "scripts": {
    "build": "npx terser app.js -o app.min.js -c -m && mv app.min.js app.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "obscenity": "^0.4.6"
  },
  "devDependencies": {
    "terser": "^5.46.1"
  }
}
